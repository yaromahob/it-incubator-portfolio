import React from 'react';
import styles from './Header.module.scss'

const Header = () => {
  return (
    <header>
      <h3>Header</h3>
      <nav></nav>
    </header>
  );
};

export default Header;
